import React, { useState } from "react";

const Addd =()=>{
const [products,setProducts] = useState([]);

const addProduct = product => setProducts([])

return(
    <div>

    </div>
);

}
export default Addd;